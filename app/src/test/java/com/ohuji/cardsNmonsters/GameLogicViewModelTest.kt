package com.ohuji.cardsNmonsters

import android.content.Context
import com.ohuji.cardsNmonsters.screens.augmented_reality.GameLogicViewModel
import junit.framework.TestCase.assertEquals
import org.junit.Before
import org.junit.Test

class GameLogicViewModelTest {

    private lateinit var viewModel: GameLogicViewModel
/*
    @Before
    fun setup() {
        val context = ApplicationProvider.getApplicationContext<Context>()
        viewModel = GameLogicViewModel(
    }

    @Test
    fun testGetStartingHealth() {
        assertEquals(2000, viewModel.getStartingHealth(1L))
        assertEquals(1200, viewModel.getStartingHealth(2L))
        assertEquals(1500, viewModel.getStartingHealth(3L))
        assertEquals(800, viewModel.getStartingHealth(4L))
        assertEquals(700, viewModel.getStartingHealth(5L))
        assertEquals(750, viewModel.getStartingHealth(6L))
        assertEquals(1000, viewModel.getStartingHealth(7L))
        assertEquals(50, viewModel.getStartingHealth(8L))
    }

    @Test
    fun testDoDamage() {
        assertEquals(10, viewModel.doDamage(5, false, "Water", null))
        assertEquals(20, viewModel.doDamage(5, false, "Fire", "Wind"))
        assertEquals(6, viewModel.doDamage(3, true, "Earth", "Electricity"))
        assertEquals(6, viewModel.doDamage(3, true, "Fire", null))
        assertEquals(10, viewModel.doDamage(5, false, "Electricity", "Water"))
        assertEquals(6, viewModel.doDamage(3, true, "Light", "Dark"))
    }

 */
}
